"""Google Calendar OAuth helper (spec §10) — Phase 2 stub.

In Phase 2 this runs the one-time OAuth consent flow and prints a refresh token to
paste into .env (GOOGLE_REFRESH_TOKEN). Not wired up in Phase 1.
"""

if __name__ == "__main__":
    raise SystemExit(
        "google_auth is a Phase 2 helper and is not implemented yet. "
        "Phase 1 runs with MOCK_INTEGRATIONS=true and needs no Google credentials."
    )
